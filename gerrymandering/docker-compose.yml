services:
  gerrymandering:
    image: ghcr.io/sidoniebouthors/gerrymandering-game-frontend:main
    container_name: gerrymandering
    restart: unless-stopped
    networks:
      - cloudflare-network # http://gerrymandering:3000

networks:
  cloudflare-network:
    external: true